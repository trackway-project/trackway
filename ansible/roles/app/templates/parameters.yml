################################################
# {{ ansible_managed }}
################################################
#
# This file is managed by Ansible.
# Do not edit this file manually.
# Any changes will be automatically reverted.
#
################################################

parameters:
    database_driver: "{{ composer.database_driver }}"
    database_host: "{{ composer.database_host }}"
    database_port: "{{ composer.database_port }}"
    database_name: "{{ composer.database_name }}"
    database_user: "{{ composer.database_user }}"
    database_password: "{{ composer.database_password }}"
    mailer_transport: "{{ composer.mailer_transport }}"
    mailer_host: "{{ composer.mailer_host }}"
    mailer_user: "{{ composer.mailer_user }}"
    mailer_password: "{{ composer.mailer_password }}"
    locale: "{{ composer.locale }}"
    secret: "{{ composer.secret }}"