language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - composer self-update
  - composer install --prefer-source --dev
  - php app/console doctrine:schema:create --force
  - mysql trackway < groups.sql

script:
  - phpunit